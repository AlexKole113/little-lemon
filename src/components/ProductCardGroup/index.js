import './index.scss'
const ProductCardGroup = ({children}) => {
    return(
        <div className="product-card-group">
            {children}
        </div>
    )
}

export default ProductCardGroup;
