import Hero from "../../components/Hero";

const PageHome = () => {

    return (
        <section className={`page page-home`}>
            <Hero />

        </section>
    )
}

export default PageHome;
